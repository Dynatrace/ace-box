- name: Ensure the easytrade namespace exists
  kubernetes.core.k8s:
    kind: Namespace
    api_version: v1
    name: "{{ easytrade_namespace }}"
    state: present

- name: Template Easytrade Ingress Kubernetes Manifest
  template:
    src: "{{ role_path }}/templates/easytrade-ingress.yml.j2"
    dest: "{{ role_path }}/files/kubernetes-manifests/easytrade-ingress.yml"
    owner: "{{ ace_box_user }}"
    group: "{{ ace_box_user }}"
    mode: '0644'

- name: Apply Kubernetes manifests using kubectl and Kustomize
  ansible.builtin.shell:
    cmd: kubectl apply -k "{{ role_path }}/files/kustomize"
  args:
    executable: /bin/bash
