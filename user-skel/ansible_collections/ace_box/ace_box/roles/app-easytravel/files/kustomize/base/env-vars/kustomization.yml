apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../kubernetes-manifests

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: mongodb
    path: kubernetes-env-vars-patch.yml