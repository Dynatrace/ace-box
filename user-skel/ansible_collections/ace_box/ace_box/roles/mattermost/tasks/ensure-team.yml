---
- name: Search for Mattermost pod
  kubernetes.core.k8s_info:
    kind: Pod
    namespace: "{{ mattermost_namespace }}"
    label_selectors:
      - app = mattermost
      - installation.mattermost.com/resource = mattermost
  register: mm_pod

- name: Create Team
  kubernetes.core.k8s_exec:
    namespace: "{{ mattermost_namespace }}"
    pod: "{{ mm_pod.resources[0].metadata.name }}"
    command: mmctl team create --name "{{ mm_team_name }}" --display-name "{{ mm_team_display_name | default(mm_team_name) }}" --local
  ignore_errors: true
