{
  "name": "{{ .name }}",
  "description": null,
  "rules": [
    {
      "enabled": true,
      "type": "ME",
      "attributeRule": {
        "entityType": "PROCESS_GROUP",
        "conditions": [
          {
            "key": "PROCESS_GROUP_TAGS",
            "operator": "EQUALS",
            "tag": "{{ .tag }}"
          }
        ],
        "pgToHostPropagation": false,
        "pgToServicePropagation": true
      }
    }
  ]
}