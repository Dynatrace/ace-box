---
attachRules:
  tagRule:
  - meTypes:
    - PROCESS_GROUP_INSTANCE
    tags:
    - context: ENVIRONMENT
      key: RELEASE_PRODUCT
      value: ${RELEASE_PRODUCT}
    - context: ENVIRONMENT
      key: RELEASE_STAGE
      value: ${RELEASE_STAGE}
    - context: ENVIRONMENT
      key: RELEASE_BUILD_VERSION
      value: ${RELEASE_BUILD_VERSION}
