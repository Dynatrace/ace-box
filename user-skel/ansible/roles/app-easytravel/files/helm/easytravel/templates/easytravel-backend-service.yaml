apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-backend
  name: {{ .Chart.Name }}-backend
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: {{ .Chart.Name }}-backend
  type: ClusterIP
