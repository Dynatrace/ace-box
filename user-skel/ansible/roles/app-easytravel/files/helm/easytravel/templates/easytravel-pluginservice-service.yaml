apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Chart.Name }}-pluginservice
  name: {{ .Chart.Name }}-pluginservice
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: {{ .Chart.Name }}-pluginservice
  type: ClusterIP
